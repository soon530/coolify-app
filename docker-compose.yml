services:
  hello-pulo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PUBLIC_PORT:-8080}:3000"
    environment:
      HOST: "${HOST:-0.0.0.0}"
      PORT: "${PORT:-3000}"
